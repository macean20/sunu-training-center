# SUNU Training Center — Redirections Cloudflare Pages
# NOTE: ne pas pointer vers des URLs en .html — Cloudflare Pages (Pretty URLs)
# redirige automatiquement /foo.html → /foo, ce qui crée une boucle infinie.

# Compatibilité URL sans .html
/accueil          /                  301
/about            /a-propos          301
/apropos          /a-propos          301
/service          /services          301
/formation        /formations        301
/etudiant         /etudiants         301
/partenaire       /partenaires       301
/dashboard        /app               301
/login            /app               301

# Fallback SPA (ne pas activer, site statique multipage)
# /*  /index.html  200
